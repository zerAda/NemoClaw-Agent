---
phase: 04-telegram-interface-and-autonomous-scheduling
plan: 03
type: execute
wave: 2
depends_on: [01-05]
files_modified:
  - career_agent/src/alerts.py
  - career_agent/src/scraper.py
  - career_agent/src/app.py
autonomous: true
requirements: [TELE-02, TELE-06]
must_haves:
  truths:
    - "New class AlertService(brain_path=brain_path) implemented"
    - "AlertService.notify(level, message, source) sends a push to NemoBot"
    - "Scraper.py catch-blocks invoke alert_service.notify() on Captcha/Blocked/Failures"
    - "Alert messages are formatted for Telegram (Source, Level, Message)"
  artifacts:
    - path: "career_agent/src/alerts.py"
      provides: "New alerting service for real-time notification"
    - path: "career_agent/src/scraper.py"
      provides: "Updated catch-blocks with alerting integration"
---

<objective>
Implement the `AlertService` to provide real-time visibility into the health of the scrapers. Without this, silent failures (like Captchas) would prevent autonomous progress without the user's knowledge.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/02-stealth-layer-and-scraping-foundation/02-05-SUMMARY.md
</context>

<interfaces>
From career_agent/src/alerts.py:
```python
class AlertService:
    def __init__(self, brain_path: str): ...
    async def notify(self, level: str, message: str, source: str = "SYSTEM"): ...
```
</interfaces>

<steps>
1.  **AlertService Logic**: Implement a basic HTTP poster to the NemoBot container's internal `/push` endpoint (to be added) or direct Telegram API if token is shared.
2.  **Scraper Integration**: Wrap platform-specific scraping methods in `try-except` blocks that call the `AlertService`.
3.  **Bot Update**: Ensure NemoBot has a webhook or internal endpoint to receive and broadcast these alerts to the bot owner.
4.  **Verification**: Manually trigger a "fake" failure and verify the Telegram message arrives.
</steps>
