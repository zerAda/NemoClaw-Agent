---
phase: 04-telegram-interface-and-autonomous-scheduling
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - career_agent/src/app.py
  - career_agent/requirements.txt
autonomous: true
requirements: [TELE-01, TELE-05]
must_haves:
  truths:
    - "Library `apscheduler` added to `requirements.txt`"
    - "AsyncIOScheduler starts within Sidecar lifespan"
    - "Daily job triggers `run_cycle()` once per 24h at a configurable hour (default 09:00)"
    - "System respects the Pause/Resume flag from Plan 04-01"
  artifacts:
    - path: "career_agent/src/app.py"
      provides: "Autonomous scheduler initialization and worker loop"
---

<objective>
Integrate `apscheduler` into `career_agent/src/app.py` to provide a truly autonomous processing clock. This removes the need for human-triggered cycles and enables the system to run while the user is away.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/04-telegram-interface-and-autonomous-scheduling/04-01-PLAN.md
</context>

<interfaces>
From career_agent/src/app.py:
```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler

scheduler = AsyncIOScheduler()
scheduler.add_job(run_cycle, 'cron', hour=9, minute=0)
scheduler.start()
```
</interfaces>

<steps>
1.  **Add Dependencies**: Update `career_agent/requirements.txt` with `apscheduler`.
2.  **Scheduler Setup**: In `app.py`, initialize the `AsyncIOScheduler`.
3.  **Cron Task**: Map the scheduler to call the existing `run_cycle()` function.
4.  **Control Integration**: Ensure `run_cycle()` checks the `ControlGate.is_paused()` status before proceeding.
5.  **Verification**: Set a job to run in 1 minute and verify the logs show the autonomous start.
</steps>
