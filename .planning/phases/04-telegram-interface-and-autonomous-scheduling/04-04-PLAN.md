---
phase: 04-telegram-interface-and-autonomous-scheduling
plan: 04
type: execute
wave: 2
depends_on: [04-02, 05-01]
files_modified:
  - career_agent/src/app.py
  - career_agent/src/memory.py
autonomous: true
requirements: [TELE-01]
must_haves:
  truths:
    - "New job in Scheduler: `daily_summary` at 20:00"
    - "Summary function queries MemoryService for status='APPLIED' and score >= 0.85"
    - "Message formatted as Markdown for Telegram"
    - "System provides counts for: Scraped, Scored, Tailored, Applied"
  artifacts:
    - path: "career_agent/src/app.py"
      provides: "Daily summary generation and transmission logic"
---

<objective>
Automate the daily digest report to provide the user with a low-friction summary of the bot's autonomous progress. This ensures the user stays informed without needing to manually query `/status`.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/04-telegram-interface-and-autonomous-scheduling/04-02-PLAN.md
</context>

<interfaces>
From career_agent/src/memory.py:
```python
class MemoryService:
    def get_stats(self, days: int = 1) -> Dict: ...
```
</interfaces>

<steps>
1.  **Stats Persistence**: Add a `get_stats()` method to `MemoryService` that queries Qdrant/SQLite for activity within the last 24 hours.
2.  **Summary Trigger**: Add a second cron job to `AsyncIOScheduler` to trigger the summary message (e.g., at 8:00 PM).
3.  **Message Formatting**: Draft a template that includes key metrics (New Matches, Applications Sent, Active Follow-ups).
4.  **Verification**: Manually trigger the summary and verify the Telegram broadcast.
</steps>
