---
phase: 01-sidecar-foundation
plan: 05
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - skills/career_agent/SKILL.md
  - agent.md
  - .github/workflows/nemoclaw-ci.yml
autonomous: false
requirements: [INFRA-02, INFRA-04]

must_haves:
  truths:
    - "skills/career_agent/SKILL.md is valid YAML with name, description, and tools list"
    - "Each tool in SKILL.md has name, description, method, and url fields"
    - "agent.md references the career_agent skill so NemoClaw knows about job-hunting capabilities"
    - "CI installs career_agent/requirements.txt so flake8 finds all imports and no F821 errors occur"
    - "CI passes on new career_agent/sidecar/main.py without undefined name errors"
    - "CD pipeline completes after merge and 'docker ps' on VPS confirms career-agent container is healthy"
  artifacts:
    - path: "skills/career_agent/SKILL.md"
      provides: "OpenClaw skill definition for career agent HTTP integration"
      contains: "http://career-agent:8001"
    - path: "agent.md"
      provides: "NemoClaw agent identity with career_agent skill reference"
      contains: "career_agent"
    - path: ".github/workflows/nemoclaw-ci.yml"
      provides: "CI pipeline that installs career_agent deps before linting"
      contains: "career_agent/requirements.txt"
  key_links:
    - from: "skills/career_agent/SKILL.md"
      to: "career_agent/sidecar/main.py"
      via: "url: http://career-agent:8001/run-cycle (POST)"
      pattern: "career-agent:8001"
    - from: ".github/workflows/nemoclaw-ci.yml"
      to: "career_agent/requirements.txt"
      via: "pip install -r career_agent/requirements.txt"
      pattern: "career_agent/requirements.txt"
---

<objective>
Register the career_agent skill with OpenClaw (INFRA-02) by creating skills/career_agent/SKILL.md and updating agent.md. Update the CI pipeline to install career_agent/requirements.txt so flake8 can resolve all imports in the new sidecar files (INFRA-04). Gate INFRA-04 completion on a CD verification checkpoint confirming the career-agent container actually runs healthy on the VPS.

Purpose: Without SKILL.md NemoClaw cannot call the sidecar over HTTP regardless of whether both containers are running. Without the CI fix, flake8 will fail on F821 undefined names in career_agent/sidecar/main.py. Without CD verification, INFRA-04 is only confirmed at the CI level — not the deployment level.
Output: skills/career_agent/SKILL.md, updated agent.md, updated nemoclaw-ci.yml. Tests for SKILL.md schema validation activated. CD verification checkpoint before INFRA-04 is marked complete.
</objective>

<execution_context>
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-sidecar-foundation/01-RESEARCH.md
@.planning/phases/01-sidecar-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Sidecar endpoints defined by Plan 03 (career_agent/sidecar/main.py) -->
GET  http://career-agent:8001/health     → {"status": "ok"}
POST http://career-agent:8001/run-cycle  → 202 {"status": "accepted", "message": "Cycle started"}
                                           Body: {"keyword": string, "location": string}

<!-- OpenClaw SKILL.md schema (from research Pattern 3) -->
Required fields per tool:
  - name: string
  - description: string
  - method: GET | POST
  - url: string (full http URL)
  - parameters: (optional) map of param_name -> {type, required, description}

<!-- CI current "Install dependencies" step -->
run: |
  python -m pip install --upgrade pip
  pip install flake8 bandit python-dotenv
  if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
  # Missing: no career_agent/requirements.txt install
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create skills/career_agent/SKILL.md and activate skill schema tests</name>
  <files>skills/career_agent/SKILL.md, career_agent/tests/test_skill_md.py</files>
  <read_first>
    - career_agent/tests/test_skill_md.py (stubs to activate — remove skip markers and implement)
    - .planning/phases/01-sidecar-foundation/01-RESEARCH.md (Pattern 3: OpenClaw SKILL.md Definition + Code Examples: OpenClaw SKILL.md)
    - .planning/phases/01-sidecar-foundation/01-03-SUMMARY.md (confirms /health and /run-cycle endpoint paths)
  </read_first>
  <behavior>
    - Test 1: skills/career_agent/SKILL.md parses as valid YAML without errors
    - Test 2: Each entry in the tools list has the required fields: name, description, method, url
    - Test 3: URL values point to http://career-agent:8001 (correct Docker service DNS name)
  </behavior>
  <action>
    **Step 1 — Create skills/career_agent/SKILL.md**:

    First create the skills/career_agent/ directory by writing the file (the directory is implicit).

    ```yaml
    name: career_agent
    description: >
      Autonomous job hunting tool. Use this skill to run a job search and scoring
      cycle, retrieve current application pipeline status, or verify the career
      agent is running. The career agent scrapes job listings, scores them against
      the candidate profile in /app/brain/Bio_Context.md, and generates tailored
      cover letters for high-scoring matches.
    tools:
      - name: run_cycle
        description: >
          Start a job search, scoring, and cover letter generation cycle.
          Call when the user asks to search for jobs, run the career agent,
          or trigger Phoenix. Returns immediately (202) — cycle runs in background.
        method: POST
        url: http://career-agent:8001/run-cycle
        parameters:
          keyword:
            type: string
            required: true
            description: "Job title or keyword to search for (e.g. 'AI Engineer', 'Backend Developer')"
          location:
            type: string
            required: false
            default: "France"
            description: "Target job location"

      - name: health_check
        description: >
          Verify the career agent sidecar is running and ready to accept requests.
          Call before run_cycle if you are unsure the service is up.
        method: GET
        url: http://career-agent:8001/health
    ```

    **Step 2 — Activate test_skill_md.py** (replace skip stubs with real tests):
    ```python
    import pytest
    import yaml
    import os


    SKILL_MD_PATH = os.path.join(
        os.path.dirname(__file__), "..", "..", "skills", "career_agent", "SKILL.md"
    )


    @pytest.fixture
    def skill_data():
        with open(SKILL_MD_PATH, "r") as f:
            return yaml.safe_load(f)


    def test_skill_md_is_valid_yaml(skill_data):
        """skills/career_agent/SKILL.md parses as valid YAML."""
        assert skill_data is not None
        assert isinstance(skill_data, dict)


    def test_skill_md_has_required_top_level_fields(skill_data):
        """SKILL.md has name, description, and tools fields."""
        assert "name" in skill_data
        assert "description" in skill_data
        assert "tools" in skill_data
        assert isinstance(skill_data["tools"], list)
        assert len(skill_data["tools"]) >= 1


    def test_skill_md_has_required_tool_fields(skill_data):
        """Each tool entry has name, description, method, and url fields."""
        required = {"name", "description", "method", "url"}
        for tool in skill_data["tools"]:
            missing = required - set(tool.keys())
            assert not missing, f"Tool '{tool.get('name', '?')}' missing fields: {missing}"


    def test_skill_md_urls_point_to_career_agent_service(skill_data):
        """All tool URLs use the career-agent Docker service DNS name."""
        for tool in skill_data["tools"]:
            assert "career-agent:8001" in tool["url"], (
                f"Tool '{tool['name']}' URL must use career-agent:8001, got: {tool['url']}"
            )
    ```
  </action>
  <verify>
    <automated>python -m pytest "C:/Users/mon pc/Desktop/nemoclaw-standalone/career_agent/tests/test_skill_md.py" -x -q --tb=short 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - skills/career_agent/SKILL.md exists
    - `grep "career-agent:8001" skills/career_agent/SKILL.md` returns at least 2 matches (one per tool)
    - `grep "run_cycle\|run-cycle" skills/career_agent/SKILL.md` returns a match
    - `grep "health_check\|/health" skills/career_agent/SKILL.md` returns a match
    - pytest test_skill_md.py passes: 4 passed, 0 failed, 0 skipped
  </acceptance_criteria>
  <done>SKILL.md is valid YAML with all required fields. Tools point to correct Docker service DNS. Tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update agent.md to reference career_agent skill and fix CI to install sidecar deps</name>
  <files>agent.md, .github/workflows/nemoclaw-ci.yml</files>
  <read_first>
    - agent.md (full file — understand current content before appending skill reference)
    - .github/workflows/nemoclaw-ci.yml (full file — find exact "Install dependencies" step to update)
    - .planning/phases/01-sidecar-foundation/01-RESEARCH.md (Pitfall 5: CI F82 errors; CI Fix code example)
  </read_first>
  <action>
    **Part A — Update agent.md**:

    Read the full current content of agent.md first. Then append a career_agent skills section at the end of the file. Do NOT replace existing content — add to it.

    Append exactly this block:

    ```markdown

    ## Available Skills

    ### career_agent
    You have access to an autonomous job hunting tool via the `career_agent` skill.

    **When to use:**
    - User asks to search for jobs, run the career agent, or "trigger Phoenix"
    - User asks for pipeline status or what applications are in progress
    - User asks if the career agent is running

    **Available actions:**
    - `run_cycle` — Start a job search + scoring cycle (POST /run-cycle). Requires `keyword` parameter.
    - `health_check` — Verify the sidecar is running (GET /health).

    **Important:** `run_cycle` returns 202 immediately and runs in the background. Inform the user the cycle has started, not that it has completed.
    ```

    **Part B — Fix nemoclaw-ci.yml Install dependencies step**:

    Find the "Install dependencies" step in .github/workflows/nemoclaw-ci.yml. It currently ends with:
    ```yaml
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    ```

    Add one line after that existing line:
    ```yaml
          if [ -f career_agent/requirements.txt ]; then pip install -r career_agent/requirements.txt; fi
    ```

    The complete updated step run block becomes:
    ```yaml
        run: |
          python -m pip install --upgrade pip
          pip install flake8 bandit python-dotenv
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f career_agent/requirements.txt ]; then pip install -r career_agent/requirements.txt; fi
    ```

    Do NOT change any other part of nemoclaw-ci.yml.
  </action>
  <verify>
    <automated>grep -c "career_agent/requirements.txt\|career_agent skill\|run_cycle" "C:/Users/mon pc/Desktop/nemoclaw-standalone/.github/workflows/nemoclaw-ci.yml" "C:/Users/mon pc/Desktop/nemoclaw-standalone/agent.md" 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep "career_agent/requirements.txt" .github/workflows/nemoclaw-ci.yml` returns exactly one match
    - The CI step still contains the original `if [ -f requirements.txt ]` line (not removed)
    - `grep "career_agent" agent.md` returns at least one match
    - `grep "run_cycle" agent.md` returns at least one match
    - `grep "202" agent.md` returns at least one match (the note about 202 immediate return)
    - agent.md file is longer than before (content was appended, not replaced)
  </acceptance_criteria>
  <done>agent.md tells NemoClaw how and when to use the career_agent skill. CI installs career_agent/requirements.txt so flake8 can resolve all imports in sidecar/main.py — no F821 errors on push to master.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CD verification — confirm career-agent container healthy on VPS</name>
  <what-built>
    Plans 01-05 complete the full sidecar foundation: FastAPI sidecar, Dockerfile, docker-compose.yml
    with career-agent service, SKILL.md, and CI pipeline fix. This checkpoint gates INFRA-04 completion
    on actual deployment verification, not just CI passing.
  </what-built>
  <how-to-verify>
    1. Ensure all plan changes are committed and pushed to master.
    2. Visit GitHub Actions for this repo and wait for the nemoclaw-cd.yml workflow run to complete.
       Confirm it shows a green checkmark (all steps passed including SSH deploy).
    3. SSH into the VPS and run:
       ```
       docker ps --filter name=career-agent --format "table {{.Names}}\t{{.Status}}"
       ```
       Confirm output shows `career-agent` with status `Up ... (healthy)`.
    4. Optionally run a quick smoke test from the VPS:
       ```
       curl -s http://localhost:8001/health
       ```
       Confirm response is `{"status":"ok"}`.
    5. Send a Telegram message to NemoClaw such as "run a health check on the career agent".
       Then on the VPS run:
       ```
       docker logs career-agent --tail=20
       ```
       Confirm the logs show an incoming HTTP request to `/health` (e.g. a FastAPI access log line
       like `GET /health HTTP/1.1" 200`). This verifies the full end-to-end path:
       Telegram → OpenClaw skill dispatch → HTTP → career-agent sidecar.

    **If NemoClaw does not invoke the skill (skill-not-loading branch):**
    - Check that the skills directory env var is set correctly inside the openclaw container:
      ```
      docker exec openclaw env | grep SKILL
      ```
      Note the exact variable name returned (e.g. `OPENCLAW_SKILLS_PATH`, `SKILLS_PATH`, or similar).
      If it differs from what docker-compose.yml sets, update the env var name in docker-compose.yml
      and redeploy.
    - If the variable is absent, try alternative common values: `/app/skills`, `/skills`,
      `/app/skills/career_agent/SKILL.md` and redeploy to test each.
    - Inspect the openclaw container logs for skill registration errors at startup:
      ```
      docker logs openclaw --tail=50 | grep -i skill
      ```
      Look for lines containing "skill", "load", "register", or "error" to identify why
      the career_agent skill was not picked up.
  </how-to-verify>
  <resume-signal>
    Type "verified" if docker ps shows career-agent healthy, CD completed successfully, and
    docker logs career-agent confirms an incoming /health request after the Telegram message.
    Type "cd-failed" plus the error if the CD workflow failed.
    Type "container-unhealthy" plus docker logs output if the container is not healthy.
    Type "skill-not-loading" plus the output of `docker exec openclaw env | grep SKILL` and
    `docker logs openclaw --tail=50 | grep -i skill` if NemoClaw did not invoke the skill.
  </resume-signal>
</task>

</tasks>

<verification>
After Wave 2, Plan 05:
- `python -m pytest career_agent/tests/ -x -q` passes: all activated tests green
- `python -m pytest career_agent/tests/ -v` shows: test_sidecar (3 passed), test_memory_path (2 passed), test_skill_md (4 passed)
- `grep "career_agent/requirements.txt" .github/workflows/nemoclaw-ci.yml` returns one match
- `grep "career-agent:8001" skills/career_agent/SKILL.md` returns matches
- `grep -r "gemini-1.5" career_agent/ docker-compose.yml` returns zero lines (from Plan 02)
- CD checkpoint verified: career-agent container healthy on VPS (Task 3 gate passed)

Full phase gate checks:
- All 9 tests pass (no skips remain)
- Zero gemini-1.5-* strings in repo
- skills/career_agent/SKILL.md is valid YAML
- docker-compose.yml has two services on traefik-net sharing ./brain volume
- docker-compose.yml nemoclaw service has ./skills:/app/skills:ro mount
- CI pipeline installs career_agent/requirements.txt
- CD pipeline confirmed deploying career-agent container healthy on VPS (INFRA-04 fully gated)
</verification>

<success_criteria>
Phase 1 complete when:
1. SKILL.md registered and tests pass (INFRA-02)
2. Both services on traefik-net sharing ./brain; nemoclaw has skills volume (INFRA-03)
3. CI installs sidecar deps and flake8 passes on sidecar/main.py (INFRA-04 — CI gate)
4. Zero gemini-1.5-* strings in repo (INFRA-06)
5. FastAPI /health and /run-cycle endpoints tested and passing (INFRA-01)
6. CD pipeline confirmed: career-agent container healthy on VPS (INFRA-04 — deployment gate, Task 3 checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-sidecar-foundation/01-05-SUMMARY.md`
</output>
