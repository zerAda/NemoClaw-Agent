---
phase: 08-auto-apply-service-and-legal-gate
plan: 02
type: execute
wave: 1
depends_on: [06-02, 08-01]
files_modified:
  - career_agent/src/submitter.py
autonomous: true
requirements: [SUBMIT-02, SUBMIT-03, SUBMIT-04, SUBMIT-05]
must_haves:
  truths:
    - "SubmitterService supports `apply_wttj` and `apply_apec` via Playwright"
    - "Human-like typing delays (50-200ms) implemented for form fields"
    - "Selector strategy prioritizes stable ARIA labels"
    - "Randomized delays (5-15s) between form pages"
    - "Screenshot captured of the confirmation page"
  artifacts:
    - path: "career_agent/src/submitter.py"
      provides: "New Playwright-based form-fillers for multi-platform submission"
---

<objective>
Implement the Playwright-based form fillers for Welcome to the Jungle and APEC. This enables autonomous applications on platforms where official APIs are not available, using high-fidelity behavioral mimicry to avoid bot detection (SUBMIT-02, SUBMIT-03).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/02-stealth-layer-and-scraping-foundation/02-02-SUMMARY.md
</context>

<interfaces>
From career_agent/src/submitter.py:
```python
class SubmitterService:
    async def apply_wttj(self, job_url: str, pdf_path: str): ...
    async def apply_apec(self, job_url: str, pdf_path: str): ...
```
</interfaces>

<steps>
1.  **Behavioral Mimicry**: Implement a `human_type()` helper and randomized click paths.
2.  **Form Analysis**: Map the mandatory fields (Name, Email, Phone, CV, Letter, Links) for both WTTJ and APEC.
3.  **Error Recovery**: Implement fallback logic for common form errors (e.g., "Field Required") and trigger the `AlertService`.
4.  **Verification**: Manual run on a "sandbox" or test job listing on each platform.
</steps>
