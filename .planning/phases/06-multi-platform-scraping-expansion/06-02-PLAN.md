---
phase: 06-multi-platform-scraping-expansion
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - career_agent/src/scraper.py
autonomous: true
requirements: [SCRAPE-03]
must_haves:
  truths:
    - "New class APECScraper(brain_path=brain_path) implemented in `scraper.py`"
    - "APECScraper supports search by Query and Location"
    - "APECScraper returns list of `JobListing` objects"
    - "APECScraper uses persistent headers from Playwright session"
  artifacts:
    - path: "career_agent/src/scraper.py"
      provides: "New scraper for APEC (Association Pour l'Emploi des Cadres)"
---

<objective>
Implement the `APECScraper` to target executive and managerial-level roles across France. APEC is the premier platform for professional and technical roles in the French ecosystem (SCRAPE-03).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/06-multi-platform-scraping-expansion/06-01-PLAN.md
</context>

<interfaces>
From career_agent/src/scraper.py:
```python
class APECScraper:
    def __init__(self, brain_path: str): ...
    async def search(self, query: str, location: str) -> List[JobListing]: ...
```
</interfaces>

<steps>
1.  **URL Discovery**: Research current APEC URL patterns for job searches.
2.  **Scraper Logic**: Implement `search()` using Playwright to extract Job Title, Company, Description, and the unique ID.
3.  **Pipeline Wiring**: Add `APECScraper` to `run_cycle()` in `app.py`.
4.  **Verification**: Manual run for "Tech Lead" in Lyon should return APEC results correctly and pass them to scoring.
</steps>
