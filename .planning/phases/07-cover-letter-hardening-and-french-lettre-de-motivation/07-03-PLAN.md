---
phase: 07-cover-letter-hardening-and-french-lettre-de-motivation
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - career_agent/src/tailor.py
  - career_agent/src/app.py
autonomous: true
requirements: [APPLY-03, APPLY-05]
must_haves:
  truths:
    - "TailorService includes a post-generation validation step"
    - "Validation checks for keywords present in generated text against Bio_Context.md"
    - "Validation rejects letter if Role Title or Company Name is missing from text"
    - "A failed validation triggers a Telegram Alert (from Phase 4) and prevents application"
  artifacts:
    - path: "career_agent/src/tailor.py"
      provides: "New post-generation validation gate for cover letters"
---

<objective>
Implement the `ValidationService` to ensure the integrity of AI-generated cover letters. Autonomous systems must have a "Trust but Verify" layer to prevent professional damage caused by AI hallucinations (APPLY-03, APPLY-05).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/07-cover-letter-hardening-and-french-lettre-de-motivation/07-01-PLAN.md
</context>

<interfaces>
From career_agent/src/tailor.py:
```python
class TailorService:
    async def validate(self, letter: str) -> bool: ...
```
</interfaces>

<steps>
1.  **Skill Registry**: Pre-load all valid skills and experiences from `Bio_Context.md` and `Target_Specs.json`.
2.  **Cross-Check Logic**: Implement a "Check for Hallucinations" step that verifies any claims made in the letter against the known candidate profile.
3.  **Mandatory Field Check**: Ensure the Role Title and Company Name are detected in the generated text.
4.  **Failure Handling**: Ensure failures are surfaced to the `AlertService` and the job's tracking status is updated to "FAILED_VALIDATION".
5.  **Verification**: Manual test with a "deliberately hallucinated" mock letter should be blocked.
</steps>
