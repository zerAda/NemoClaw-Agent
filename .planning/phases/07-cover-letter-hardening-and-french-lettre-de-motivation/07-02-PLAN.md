---
phase: 07-cover-letter-hardening-and-french-lettre-de-motivation
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - career_agent/src/pdf_service.py
  - career_agent/requirements.txt
autonomous: true
requirements: [APPLY-04]
must_haves:
  truths:
    - "New library `reportlab` or `weasyprint` added to `requirements.txt`"
    - "New class `PDFService` generated formatted PDFs"
    - "PDFs include header with name, contact info, and role title"
    - "Output saved to `brain/output/` directory"
  artifacts:
    - path: "career_agent/src/pdf_service.py"
      provides: "New service for PDF document generation"
---

<objective>
Implement the `PDFService` to convert generated cover letters into the final format required for application attachments. Standard PDF generation ensures consistent professional styling (APPLY-04).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/07-cover-letter-hardening-and-french-lettre-de-motivation/07-01-PLAN.md
</context>

<interfaces>
From career_agent/src/pdf_service.py:
```python
class PDFService:
    def __init__(self, brain_path: str): ...
    def generate(self, content: TailoredContent, output_path: str) -> str: ...
```
</interfaces>

<steps>
1.  **Add Dependencies**: Update `career_agent/requirements.txt` with `reportlab`.
2.  **PDF Template Design**: Implement a clean, professional template with standard margins and typography.
3.  **Header Injection**: Wire the service to read the candidate's name and contact info from `Target_Specs.json` or `Bio_Context.md`.
4.  **Verification**: Generate a test PDF from a mock "Lettre de Motivation" and verify formatting and readability.
</steps>
