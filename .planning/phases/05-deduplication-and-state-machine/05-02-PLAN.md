---
phase: 05-deduplication-and-state-machine
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - career_agent/src/tracking.py
  - career_agent/src/models.py
autonomous: true
requirements: [TRACK-02, TRACK-05]
must_haves:
  truths:
    - "Title normalization handles lowercase, trim, and removal of common separators"
    - "Company normalization handles 'SARL', 'SA', 'Inc.', and common suffixes"
    - "Fingerprint generated as SHA256(norm_title + '|' + norm_company)"
    - "TrackingService.is_duplicate(fingerprint) returns True if found in DB"
  artifacts:
    - path: "career_agent/src/tracking.py"
      provides: "New fingerprinting and deduplication check methods"
---

<objective>
Implement the core deduplication logic to prevent NemoClaw from applying to the same job twice across different platforms. This is critical for maintaining professional reputation and avoiding wasteful API calls (TRACK-02).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/05-deduplication-and-state-machine/05-01-PLAN.md
</context>

<interfaces>
From career_agent/src/tracking.py:
```python
def generate_fingerprint(title: str, company: str) -> str: ...
async def check_duplicate(title: str, company: str) -> bool: ...
```
</interfaces>

<steps>
1.  **Normalization Engine**: Write a robust utility to strip common noise from Job Titles (e.g., "(M/F)", "[Remote]") and Company Names (e.g., legal suffixes).
2.  **Fingerprint Generation**: Combine normalized fields and hash them to create a unique identifier for the position itself, independent of the URL.
3.  **Database Integration**: Update the `TrackingService` to use this fingerprint as a lookup key.
4.  **Verification**: Manual test with three identical jobs from different URLs should result in one unique fingerprint.
</steps>
