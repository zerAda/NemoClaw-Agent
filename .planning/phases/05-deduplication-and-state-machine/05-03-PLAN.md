---
phase: 05-deduplication-and-state-machine
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - career_agent/src/app.py
  - career_agent/src/memory.py
autonomous: true
requirements: [TRACK-01, TRACK-02, TRACK-03, SUBMIT-06]
must_haves:
  truths:
    - "New TrackingService(brain_path=brain_path) initialized in PhoenixApp"
    - "process_job() performs a fingerprint check before calling Gemini API"
    - "If duplicate detected, job is marked SKIPPED (Duplicate) and execution stops"
    - "If new, job is inserted into Tracking DB with status='SCRAPED' and continues"
    - "ScoreRecord results from Phase 3 are mirrored in the Tracking DB state"
  artifacts:
    - path: "career_agent/src/app.py"
      provides: "Updated orchestrator with deduplication gate"
---

<objective>
Wire the `TrackingService` into the main `PhoenixApp` to enforce the state machine. This is the critical gate that ensures every job is uniquely identified and tracked, preventing wasteful operations and redundant communications (TRACK-01, TRACK-02).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/05-deduplication-and-state-machine/05-01-PLAN.md
</context>

<interfaces>
From career_agent/src/app.py:
```python
async def process_job(self, listing: JobListing):
    fingerprint = generate_fingerprint(listing.title, listing.company)
    if await self.tracking.is_duplicate(fingerprint):
        return # SKIP
    ...
```
</interfaces>

<steps>
1.  **Service Integration**: In `app.py`, initialize the `TrackingService` alongside `HunterService` and `MemoryService`.
2.  **Fingerprint Gate**: At the beginning of `process_job()`, generate the fingerprint and check for duplicates in the `Tracking DB`.
3.  **State Management**: Update the tracking record's status as the job moves through Scoring and Tailoring steps.
4.  **Verification**: Manual run of the pipeline with a known duplicate should log "Skipping duplicate job" after the first run.
</steps>
