---
phase: 09-follow-up-automation-and-negotiation-support
plan: 01
type: execute
wave: 1
depends_on: [08-03, 05-01]
files_modified:
  - career_agent/src/app.py
  - career_agent/src/followup.py
autonomous: true
requirements: [FOLLOW-01, FOLLOW-02, FOLLOW-03, FOLLOW-04]
must_haves:
  truths:
    - "New class FollowupService(brain_path=brain_path) implemented"
    - "Follow-up logic: Query Tracking DB for APPLIED status older than 10 days"
    - "Follow-up logic: Gemini generates contextual follow-up message in original application language"
    - "State Machine updated: APPLIED -> FOLLOWED_UP (Max 1 follow-up)"
  artifacts:
    - path: "career_agent/src/followup.py"
      provides: "New service for automated application follow-ups"
---

<objective>
Implement the `FollowupService` to automate the follow-up process. Most recruiters respond to follow-ups, and automating this step ensures no application is abandoned (FOLLOW-01, FOLLOW-02).
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/05-deduplication-and-state-machine/05-01-PLAN.md
</context>

<interfaces>
From career_agent/src/followup.py:
```python
class FollowupService:
    def __init__(self, brain_path: str): ...
    async def run_cycle(self): ... # find old apps and send one follow-up via LinkedIn DM or Email
```
</interfaces>

<steps>
1.  **Drafting Logic**: Create a template that references the specific Role and Company from the original ScoreRecord.
2.  **Language Mirroring**: Ensure the follow-up language (FR/EN) matches the original cover letter.
3.  **Transmission**: Send the message via the platform DM (LinkedIn) or search for a recruiter contact email.
4.  **State Logic**: Update the `Tracking DB` status to "FOLLOWED_UP" to prevent multiple messages.
5.  **Verification**: Manual run on an application from 11 days ago should trigger a follow-up.
</steps>
